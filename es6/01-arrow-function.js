const square = (num) => {
  return num * num;
};

const squares = (num) => num * num;

function saludar(saludar) {
  return "Hola" + saludar + "!!";
}

console.log(saludar("Andrea"));

const saludarPersona = (persona) => "hola" + persona + "Dos";
console.log(saludarPersona("Luisa"));
